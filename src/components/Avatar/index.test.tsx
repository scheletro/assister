import React from 'react';
import { render } from '@testing-library/react';
import { Avatar } from '.';

test('renders avatar component', () => {
    render(<Avatar name="avatar" image="" style={{}} />);
});
